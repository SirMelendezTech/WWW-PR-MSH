---
import { render } from "astro:content";
import BlogLayout from "../../layouts/BlogLayout.astro";
import { getBlogPosts, relatedPosts, alternateLocaleHref } from "../../lib/blog";

export async function getStaticPaths() {
  const posts = await getBlogPosts("en");
  return posts.map((post) => ({
    params: { slug: post.id },
    props: { post, allPosts: posts },
  }));
}

const { post, allPosts } = Astro.props;
const { Content } = await render(post);
const related = relatedPosts(post, allPosts);
// null when this post has no Spanish translation yet — the header falls back to
// the Spanish blog index, and no hreflang alternate is emitted.
const altLocaleHref = await alternateLocaleHref(post, "es");
---
<BlogLayout
  title={post.data.title}
  description={post.data.description}
  author={post.data.author}
  pubDate={post.data.pubDate}
  readingTime={post.data.readingTime}
  category={post.data.category}
  tags={post.data.tags}
  related={related}
  lang="en"
  altLocaleHref={altLocaleHref}
>
  <Content />
</BlogLayout>

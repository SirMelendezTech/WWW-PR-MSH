---
import TerrainSignature from "../TerrainSignature.astro";
import MeshMapPreview from "../MeshMapPreview.astro";
import type { Locale } from "../../lib/i18n";
import { localizePath } from "../../lib/i18n";
import { useStrings, categoryLabel } from "../../lib/ui-strings";
import { getBlogPosts, blogPostHref } from "../../lib/blog";
import { getNodes } from "../../lib/nodes";

interface Props { lang: Locale }
const { lang } = Astro.props;
const h = useStrings(lang).home;
const p = (href: string) => localizePath(href, lang);

const posts = (await getBlogPosts(lang)).slice(0, 3);
const { nodes: meshNodes, live: meshLive } = await getNodes();

---
<section class="hero">
  <div class="hero-terrain" aria-hidden="true"><TerrainSignature variant="hero" /></div>
  <div class="container hero-inner">
    <div class="hero-lead">
      <p class="eyebrow">{h.heroEyebrow}</p>
      <h1>{h.heroTitle}</h1>
      <p class="hero-sub">{h.heroSub1}</p>
      <div class="hero-actions">
        <a href={p("/getting-started/")} class="btn btn-primary">{h.heroCtaPrimary}</a>
        <a href={p("/pr-mesh/")} class="link-arrow hero-alt">{h.heroCtaSecondary}</a>
      </div>
    </div>
    <div class="hero-aside">
      <p class="hero-pull">{h.heroSub3}</p>
      <p class="hero-note">{h.heroSub2}</p>
    </div>
  </div>
</section>

<section class="section intro-section">
  <div class="container intro-grid">
    <div>
      <p class="eyebrow">{h.introEyebrow}</p>
      <h2>{h.introTitle}</h2>
      <p>{h.introBody1}</p>
      <p>{h.introBody2}</p>
      <a href={p("/how-it-works/")} class="link-arrow">{h.introLink}</a>
    </div>
    <div class="intro-card">
      <p class="eyebrow">{h.whyEyebrow}</p>
      <ul>
        {h.whyItems.map((item) => <li>{item}</li>)}
      </ul>
    </div>
  </div>
</section>

<section class="section">
  <div class="container">
    <div class="section-head">
      <p class="eyebrow">{h.featuresEyebrow}</p>
      <h2>{h.featuresTitle}</h2>
    </div>
    <dl class="claims">
      {h.features.map((feature) => (
        <div class="claim">
          <dt>
            <span class="claim-kicker">{feature.kicker}</span>
            <span class="claim-title">{feature.title}</span>
          </dt>
          <dd>{feature.body}</dd>
        </div>
      ))}
    </dl>
  </div>
</section>

<TerrainSignature variant="divider" />

<section class="section quickstart-section">
  <div class="container">
    <div class="section-head">
      <p class="eyebrow">{h.quickstartEyebrow}</p>
      <h2>{h.quickstartTitle}</h2>
    </div>
    <ol class="quickstart-list">
      {h.steps.map((step, i) => (
        <li>
          <span class="step-num">{String(i + 1).padStart(2, "0")}</span>
          <div>
            <h3>{step.title}</h3>
            <p>{step.body}</p>
          </div>
        </li>
      ))}
    </ol>
    <a href={p("/getting-started/")} class="btn btn-primary">{h.quickstartCta}</a>
  </div>
</section>

<section class="section map-section">
  <div class="container">
    <MeshMapPreview nodes={meshNodes} live={meshLive} lang={lang} />
  </div>
</section>

<section class="section split-section">
  <div class="container split-grid">
    <div class="split-card split-card--accent">
      <p class="eyebrow">{h.splitCommunityEyebrow}</p>
      <h2>{h.splitCommunityTitle}</h2>
      <p>{h.splitCommunityBody}</p>
      <a href={p("/community/")} class="btn btn-primary">{h.splitCommunityCta}</a>
    </div>
    <div class="split-card">
      <p class="eyebrow">{h.splitStartEyebrow}</p>
      <h2>{h.splitStartTitle}</h2>
      <p>{h.splitStartBody}</p>
      <a href={p("/getting-started/")} class="btn btn-secondary">{h.splitStartCta}</a>
    </div>
  </div>
</section>

{posts.length > 0 && (
  <section class="section">
    <div class="container">
      <div class="section-head">
        <p class="eyebrow">{h.blogEyebrow}</p>
        <h2>{h.blogTitle}</h2>
      </div>
      <ol class="post-log">
        {posts.map((post, i) => (
          <li>
            <a href={blogPostHref(lang, post.id)}>
              <span class="post-log-num" aria-hidden="true">{String(i + 1).padStart(2, "0")}</span>
              <span class="post-log-main">
                <span class="post-category">{categoryLabel(lang, post.data.category)}</span>
                <span class="post-log-title">{post.data.title}</span>
                <span class="post-log-desc">{post.data.description}</span>
                <span class="post-meta">{post.data.author} · {post.data.readingTime}</span>
              </span>
            </a>
          </li>
        ))}
      </ol>
      <a href={p("/blog/")} class="link-arrow">{h.blogReadAll}</a>
    </div>
  </section>
)}

<section class="section cta-section">
  <div class="container cta-inner">
    <h2>{h.ctaTitle}</h2>
    <p>{h.ctaBody}</p>
    <div class="hero-actions">
      <a href={p("/getting-started/")} class="btn btn-primary">{h.ctaPrimary}</a>
      <a href={p("/hardware/")} class="btn btn-secondary">{h.ctaSecondary}</a>
    </div>
  </div>
</section>

<style>
  .hero { position: relative; overflow: hidden; padding-top: 2.5rem; }
  .hero-terrain { position: absolute; inset: auto 0 0 0; height: 150px; z-index: 0; opacity: 0.6; pointer-events: none; }

  /* Two columns that deliberately do not start level: the argument sits lower
     and narrower than the explanation, so the eye reaches it second. */
  .hero-inner {
    position: relative;
    z-index: 1;
    display: grid;
    grid-template-columns: 1fr;
    gap: 2.25rem;
    padding-bottom: 8.5rem;
  }
  @media (min-width: 900px) {
    .hero-inner { grid-template-columns: 1.35fr 1fr; column-gap: 4rem; align-items: start; }
    .hero-aside { padding-top: 6.5rem; }
  }
  @media (max-width: 640px) {
    .hero-terrain { height: 110px; }
    .hero-inner { padding-bottom: 7rem; }
  }
  .hero h1 { margin-top: 0.5rem; }

  /* The three paragraphs were doing three different jobs at one size: this one
     explains, the pull states the argument, the note is an aside. */
  .hero-sub { font-size: 1.22rem; max-width: 52ch; margin: 1.1rem 0 0; color: var(--ink-soft); }
  .hero-pull {
    font-family: var(--font-display);
    font-weight: 600;
    font-size: clamp(1.25rem, 1.05rem + 0.7vw, 1.6rem);
    line-height: 1.28;
    color: var(--ink);
    border-left: 3px solid var(--accent);
    padding-left: 1.15rem;
    margin: 0;
  }
  .hero-note { margin: 1.15rem 0 0 calc(1.15rem + 3px); font-size: 1rem; max-width: 40ch; color: var(--ink-faint); }

  /* One button and one link, not two buttons: these are not equal actions. */
  .hero-actions { display: flex; flex-wrap: wrap; align-items: center; gap: 1.4rem; margin-top: 2rem; }
  .hero-alt { margin-top: 0; }
  @media (max-width: 640px) {
    .hero-actions { gap: 1.1rem; }
    .hero-actions .btn { width: 100%; }
  }

  .section { padding-block: 4rem; }
  .section-head { max-width: 60ch; margin-bottom: 2rem; }
  .section-head h2 { margin-top: 0.4rem; }

  .intro-section { background: var(--bg-sunken); border-block: 1px solid var(--border); }
  .intro-grid { display: grid; gap: 2.5rem; grid-template-columns: 1fr; align-items: start; }
  @media (min-width: 860px) { .intro-grid { grid-template-columns: 1.3fr 1fr; } }
  .intro-card {
    background: var(--bg-raised);
    border: 1px solid var(--border);
    border-radius: var(--radius-lg);
    padding: 1.75rem;
    box-shadow: var(--shadow-sm);
  }
  .intro-card ul { list-style: none; margin: 1rem 0 0; padding: 0; display: grid; gap: 0.75rem; }
  .intro-card li { padding-left: 1.4em; position: relative; color: var(--ink-soft); font-size: 0.95rem; }
  .intro-card li::before { content: "›"; position: absolute; left: 0; color: var(--secondary); font-weight: 700; }

  .link-arrow { display: inline-block; margin-top: 1.25rem; font-weight: 600; text-decoration: none; color: var(--secondary); }
  .link-arrow:hover { text-decoration: underline; }

  /* Every one of these four claims is really an absence — no tower, no
     permission, no contract, no company — so the kicker names what the mesh
     does without, and that repetition replaces four icons in tinted chips.
     Two uneven columns rather than a four-up wall of equal cards. */
  .claims { display: grid; grid-template-columns: 1fr; margin: 0; }
  @media (min-width: 780px) {
    .claims { grid-template-columns: 1.15fr 1fr; column-gap: 3.5rem; }
  }
  .claim { border-top: 1px solid var(--border); padding: 1.35rem 0 1.5rem; }
  .claim dt { margin: 0; }
  .claim-kicker {
    display: block;
    font-family: var(--font-mono);
    font-size: 0.75rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.13em;
    color: var(--accent);
    margin-bottom: 0.5rem;
  }
  .claim-title { display: block; font-family: var(--font-display); font-weight: 600; font-size: 1.15rem; color: var(--ink); }
  .claim dd { margin: 0.5rem 0 0; max-width: 46ch; color: var(--ink-soft); }

  .quickstart-list { list-style: none; margin: 0 0 2rem; padding: 0; display: grid; gap: 1.1rem; max-width: 640px; }
  .quickstart-list li { display: flex; gap: 1.1rem; align-items: flex-start; }
  .step-num { font-family: var(--font-mono); font-weight: 700; color: var(--secondary); font-size: 0.95rem; padding-top: 0.15em; flex-shrink: 0; }
  .quickstart-list h3 { font-size: 1rem; margin-bottom: 0.2rem; }
  .quickstart-list p { margin: 0; font-size: 0.92rem; }

  .split-grid { display: grid; gap: 1.5rem; grid-template-columns: 1fr; }
  @media (min-width: 780px) { .split-grid { grid-template-columns: 1fr 1fr; } }
  .split-card { border: 1px solid var(--border); border-radius: var(--radius-lg); padding: 2rem; background: var(--bg-raised); }
  .split-card--accent { background: var(--primary); color: var(--primary-ink); border-color: transparent; }
  .split-card--accent h2, .split-card--accent .eyebrow { color: var(--primary-ink); }
  .split-card--accent p { color: color-mix(in srgb, var(--primary-ink) 85%, transparent); }
  .split-card--accent .eyebrow::before { background: var(--primary-ink); }
  .split-card--accent .btn-primary { background: var(--bg-raised); color: var(--primary); }

  /* Same log treatment as the blog index — three most recent as rows against
     a numbered rail, not a card wall. */
  .post-log { list-style: none; margin: 0 0 1.5rem; padding: 0; }
  .post-log li { border-top: 1px solid var(--border); }
  .post-log a {
    display: grid;
    grid-template-columns: 1fr;
    gap: 0.15rem 1.5rem;
    padding: 1.3rem 0 1.4rem;
    text-decoration: none;
    color: inherit;
  }
  @media (min-width: 560px) {
    .post-log a { grid-template-columns: 3rem minmax(0, 1fr); }
  }
  .post-log-num { font-family: var(--font-mono); font-size: 0.8rem; color: var(--ink-faint); padding-top: 0.2rem; }
  .post-log-main { display: block; min-width: 0; }
  .post-category { font-family: var(--font-mono); font-size: 0.7rem; text-transform: uppercase; letter-spacing: 0.06em; color: var(--secondary); display: block; margin-bottom: 0.3rem; }
  .post-log-title { display: block; font-family: var(--font-display); font-weight: 600; font-size: 1.1rem; line-height: 1.25; color: var(--ink); }
  .post-log a:hover .post-log-title { color: var(--secondary); text-decoration: underline; text-underline-offset: 3px; }
  .post-log-desc { display: block; margin-top: 0.3rem; max-width: 56ch; font-size: 0.9rem; color: var(--ink-soft); }
  .post-meta { display: block; margin-top: 0.45rem; font-family: var(--font-mono); font-size: 0.72rem; color: var(--ink-faint); }

  .cta-section { background: var(--bg-sunken); border-top: 1px solid var(--border); text-align: center; }
  .cta-inner { max-width: 620px; margin-inline: auto; }
  .cta-inner .hero-actions { justify-content: center; }
</style>
